# User-specific files
*.suo
*.user
*.sln.docstates
/.vs

# Claude config
.claude

# Build results

[Dd]ebug/
[Rr]elease/
x64/
build/
[Bb]in/
[Oo]bj/

# NuGet Packages
*.nupkg
# The packages folder can be ignored because of Package Restore
**/packages/*
# except build/, which is used as an MSBuild target.
!**/packages/build/

# ffmpeg/ffprobe binaries (~200 MB each platform) — not committed to git.
# Run tools/download-ffmpeg.ps1 to populate tools/ffmpeg/ locally.
tools/ffmpeg/
/VideoContactSheet.Cli/Properties/launchSettings.json
